import styled from "styled-components"

export const Home = () =>{
    return(
        <HomePage>
            Homepage
        </HomePage>
    )
}

const HomePage = styled.div`
    padding-top: 56px;
`